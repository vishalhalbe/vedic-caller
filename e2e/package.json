{
  "name": "vedic-caller-e2e",
  "version": "1.0.0",
  "description": "Playwright API E2E tests for JyotishConnect backend",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "dotenv": "^16.4.5",
    "wait-on": "^7.2.0"
  }
}
